name: real_estate_evaluator
version: 0.1.0
inputs:
  - id: assets_csv
    type: file
    description: CSV file with asset list (address, type, customer_price, lat, lon, etc.)
outputs:
  - id: evaluated_assets
    type: json
  - id: market_comparison
    type: json
  - id: map_data
    type: json
capabilities:
  - uses_llm: false
  - uses_map: true
policy:
  egress: none
