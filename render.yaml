services:
  - type: web
    name: hugkag-ui
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run services/ui/app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
